apiVersion: v1
kind: PersistentVolume
metadata:
  name: fortify-lim
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Gi
  hostPath:
    path: /data/fortify-lim/
